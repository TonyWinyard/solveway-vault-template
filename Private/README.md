# Private

Nothing in this folder is sent to GitHub. The `.gitignore` at the top of your vault keeps it on this laptop.

This README is the one exception, so the folder still exists when you clone your vault onto another machine.
